import styled from "styled-components";
import { ProductCategory } from "../ProductCategory";

export const ProductCategoryAdd = styled(ProductCategory)`
  border: 1rem solid var(--accent);
  padding: 1rem;
`;
